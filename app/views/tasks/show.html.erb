<h3><%= @task.title %></h3>
<p><%= @task.details %></p>
<%= link_to 'Back', tasks_path%>
<%= link_to 'Edit', edit_task_path(@task) %>
<%= link_to 'Delete', task_path(@task), method: :delete, data: { confirm: 'Do you really want to?' } %>
